# Architecture

> **This page has moved.** The architecture documentation is now an organized
> cluster under [`docs/architecture/`](architecture/), and the full
> documentation hub is at [`docs/README.md`](README.md).

Start here:

- [Architecture → Overview](architecture/overview.md) — the big picture, crate map, and design principles
- [Architecture → Contracts & Plugins](architecture/contracts-and-plugins.md) — the `Provider` + `Registry` plugin system
- [Architecture → Domain Model](architecture/domain-model.md) — `Resource`, `Metadata`, `Id`, `Health`, `ResourceSpec`
- [Architecture → Scopes & Placement](architecture/scopes-and-placement.md) — the topology hierarchy and how it bounds authorization and placement
- [Architecture → Distributed Control Plane](architecture/distributed-control-plane.md) — persistence, the encrypted mesh, membership, and Raft consensus
- [Architecture → Request Lifecycle](architecture/request-lifecycle.md) — how an API call flows end to end

Or browse the complete [documentation index](README.md).
